小说和章节审核流程实现方案（影响最小化版）
1. 数据库修改与迁移
-- 步骤1: 添加审核状态字段，默认为已批准，保护现有内容
ALTER TABLE novel ADD COLUMN audit_status ENUM('pending', 'approved', 'rejected') DEFAULT 'approved';
ALTER TABLE chapter ADD COLUMN audit_status ENUM('pending', 'approved', 'rejected') DEFAULT 'approved';

-- 步骤2: 确保所有现有内容状态为approved
UPDATE novel SET audit_status = 'approved' WHERE 1=1;
UPDATE chapter SET audit_status = 'approved' WHERE 1=1;

-- 步骤3: 修改默认值为pending，只影响新创建的内容
ALTER TABLE novel ALTER COLUMN audit_status SET DEFAULT 'pending';
ALTER TABLE chapter ALTER COLUMN audit_status SET DEFAULT 'pending';
2. API修改（保持接口不变）
小说模块API调整
获取小说列表API:
保持原有接口 GET /api/novel/list
在实现中添加 WHERE audit_status = 'approved' 条件
对于原有查询参数不做任何改变
获取章节列表API:
保持原有接口 GET /api/novel/{novel_id}/chapters
普通用户调用时添加 WHERE audit_status = 'approved' 条件
当调用者是该小说作者时，返回所有章节，并包含审核状态信息
获取小说详情API:
保持原有接口 GET /api/novel/detail/{novel_id}
添加逻辑：如果小说状态是待审核/拒绝且当前用户非作者/管理员，返回404
管理模块API实现
利用现有管理模块API，确保实际实现这些已定义但未使用的接口：
获取待审核内容:
   GET /api/admin/content/{content_type}
审核内容:
   POST /api/admin/content/audit/{audit_id}
作者相关新增API（完全新增，不影响现有API）
作者查看待审核的小说和章节:
   GET /api/novel/author/pending
返回作者所有待审核/被拒绝的内容
3. 创建流程修改
小说创建流程:
保持原有 POST /api/novel/add 接口不变
在后端实现中，将新建小说的 audit_status 设为 pending
创建成功后返回新小说ID，但提示作者小说需要审核
章节创建流程:
保持原有 POST /api/novel/{novel_id}/chapters 接口不变
在后端实现中，将新建章节的 audit_status 设为 pending
创建成功后返回新章节ID，但提示作者章节需要审核
4. 用户体验优化
作者通知机制:
当内容审核状态变更时，系统发送通知给作者
在作者控制台显示内容审核状态
配置开关:
添加系统配置项 enable_content_audit（默认true）
当配置设为false时，新创建内容自动设为approved（便于开发/测试环境使用）
特殊作者权限:
支持为部分资深作者设置 exempt_from_audit 标志，允许其内容免审核发布
这是一个可选功能，用于激励优质作者
5. 实现步骤（按顺序）
数据库修改:
执行数据库修改脚本，添加字段并维护现有数据完整性
API调整:
修改小说和章节列表过滤逻辑，根据用户角色显示适当内容
修改小说和章节创建逻辑，设置初始audit_status
管理员审核功能实现:
实现管理员内容审核接口
添加审核完成后的通知机制
前端适配:
在作者控制面板添加审核状态显示
在管理员后台添加审核操作界面
添加适当的状态提示和通知显示